# Optional Google Analytics 4 measurement ID.
# Example: G-XXXXXXXXXX
NEXT_PUBLIC_GA_ID=

# Optional self-hosted Umami tracking script.
# Example: https://analytics.example.com/script.js
NEXT_PUBLIC_UMAMI_SCRIPT_URL=
# Public website ID created in Umami.
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
# Optional comma-separated hostname allowlist.
# Example: jevhub.xyz,www.jevhub.xyz
NEXT_PUBLIC_UMAMI_DOMAINS=

# Required to make /playground execute real Jev requests.
# Keep this server-side. Never expose it through NEXT_PUBLIC_*.
TYPESAFE_API_KEY=

# Optional PostgreSQL database for explicitly consented Startup Idea Validator records.
# Apply db/migrations/001_idea_validator_submissions.sql before enabling persistence.
# Keep server-side; never prefix this value with NEXT_PUBLIC_.
DATABASE_URL=
# Set to true only after scheduling `npm run db:purge-expired-ideas` daily.
JEV_IDEA_RETENTION_CLEANUP_SCHEDULED=false

# Optional bounded playground controls.
JEV_PLAYGROUND_RATE_LIMIT=20
JEV_PLAYGROUND_TIMEOUT_MS=12000
# Optional model override. Defaults to jev-latest.
TYPESAFE_DEFAULT_MODEL=

# Optional bounded Inbox Triage controls (per IP, per hour).
JEV_INBOX_RATE_LIMIT=15
JEV_INBOX_EMAIL_LIMIT=30
JEV_INBOX_TIMEOUT_MS=12000
